@{
    var payAmount = 10.33;
    var paidStatus = false;
}

<style>
    .text-red {
        color: red;
    }

    .text-green {
        color: green;
    }
</style>

<div class="row">
    <div class="col-md-12 grid-margin">
        <div class="d-flex justify-content-between flex-wrap">
            <div class="d-flex align-items-end flex-wrap">
                <div class="me-md-3 me-xl-5">
                    <h2>Billing Management</h2>
                    <br />
                    <div class="mb-md-0">
                        <a asp-controller="Program" asp-action="Create" class="btn btn-success">
                            <i class="mdi mdi-plus" style="font-size:15px"></i> &nbsp Create New Billing
                        </a>
                        <a asp-controller="" asp-action="" class="btn btn-info btn-lg">
                            Post Bill
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12 stretch-card">
        <div class="card">
            <div class="card-body">
                <p class="card-title">Parent List</p>

                <div class="d-flex align-items-end flex-wrap">
                    <div class="me-md-3 me-xl-3">
                        <p class="mb-md-0">Paid/Unpaid Filter:</p>
                    </div>
                    <div class="d-flex col-xl-2">
                        <select class="btn form-control form-control-sm" id="exampleFormControlSelect3" style="block-size:5px;">
                            <option value="all">All</option>
                            <option value="paid">Paid</option>
                            <option value="unpaid">Unpaid</option>
                        </select>
                    </div>
                </div>
                <br />

                <div class="table-responsive">
                    <table id="order-listing" class="table">
                        <thead>
                            <tr>
                                <th>Parent Name</th>
                                <th>Child Name</th>
                                <th>Phone Number</th>
                                <th>Payment Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @* @foreach (var obj in Model)
                                {
                                <tr>
                                <td>@obj.Name</td>
                                <td>dummydata</td>
                                <td>dummydata</td>
                                <td>dummydata</td>
                                <td>
                                <a onclick="" class="btn btn-danger col-sm-4 col-md-2 col-lg-4">
                                <i class="mdi mdi-delete-forever"></i> <p>Delete</p>
                                </a>
                                </td>
                                </tr>
                                } *@
                            <tr>
                                <td>dummydata</td>
                                <td>dummydata</td>
                                <td>dummydata</td>
                                <td>
                                    @if (paidStatus == false)
                                    {
                                        <span class="text-red"><b>@payAmount.ToString()</b></span>;
                                    }
                                    else
                                    {
                                        <span class="text-green"><b>paid</b></span>;
                                    }
                                </td>
                                <td>
                                    <a onclick="" class="btn btn-info btn-icon-text">
                                        Print receipt
                                        <i class="mdi mdi-printer btn-icon-append"></i>
                                    </a>
                                </td>
                            </tr>
                            <tr>
                                <td>dummydata</td>
                                <td>dummydata</td>
                                <td>dummydata</td>
                                <td>
                                    @if (!paidStatus == false)
                                    {
                                        <span class="text-red"><b>@payAmount.ToString()</b></span>;
                                    }
                                    else
                                    {
                                        <span class="text-green"><b>paid</b></span>;
                                    }
                                </td>
                                <td>
                                    <a onclick="" class="btn btn-info btn-icon-text">
                                        Print receipt
                                        <i class="mdi mdi-printer btn-icon-append"></i>
                                    </a>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>


@* function Delete(url) {
        Swal.fire({
            title: 'Are you sure?',
            text: "You won't be able to revert this!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Yes, delete it!'
        }).then((result) => {
            if (result.isConfirmed) {
                $.ajax({
                    url: url,
                    type: 'DELETE',
                    success: function (data) {
                        dataTable.ajax.reload();
                        toastr.success(data.message);
                    }
                })
            }
        })
    } *@